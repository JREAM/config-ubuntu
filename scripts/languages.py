"""Use the common module."""
from .common import apt


def dart():
    """Install dart."""
    apt('dart')


def go():
    """Install golang."""
    apt('golang')


def go():
    """Install golang."""
    apt(['php'])
